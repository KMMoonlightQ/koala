api_version = 2
name = "interactive-example"
ui = true
command = ["python3", "main.py"]

[[tools]]
name = "show_interactive_example"
description = "Show the extension's interactive controls in the terminal."
read_only = true
parameters = { type = "object", properties = {} }
